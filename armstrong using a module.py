#python script for checking armstrong number or not using module.
n=int(input("enter a number:"))
def nod(n):
    while(n!=0):
        no=no+1
        n=n//10
    return no
import armstrong as a
a.arm()
